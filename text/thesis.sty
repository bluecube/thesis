\ProvidesPackage{thesis}

\input{common-style.tex}

\OnehalfSpacing
\setsecnumdepth{subsection}
\settocdepth{subsection}
\pagestyle{plain}
\chapterstyle{hangnum}

\usepackage[hyphens]{url}

\usepackage[final]{graphicx}
\usepackage[final]{microtype}
\usepackage{paralist}
\usepackage[
	inner=25mm,
	outer=31mm,
	top=27mm,
	bottom=39mm,
	bindingoffset=10mm,
	footskip=15mm
	]{geometry}
\usepackage[
	unicode,
	final,
	colorlinks,
	linkcolor=black,
	urlcolor=black,
	citecolor=black,
	linktoc=all
	]{hyperref}
\usepackage{verbatim}

\usepackage[obeyDraft]{todonotes}
\usepackage[backend=bibtex8]{biblatex}

\usepackage{xstring}

\usepackage[nameinlink]{cleveref}
\usepackage[final]{pdfpages}
\usepackage{dirtree}

\graphicspath{{img/}}

%% Define a new style for url that will use a smaller font.
\makeatletter
\def\url@smallerstyle{%
  \@ifundefined{selectfont}{\def\UrlFont{\sf}}{\def\UrlFont{\footnotesize\ttfamily}}}
\makeatother
%% Now actually use the newly defined style.
\urlstyle{smaller}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% Algorithms

\usepackage[boxed]{algorithm2e}
\DontPrintSemicolon
\SetAlCapSkip{1em}
\SetKw{assign}{\(\leftarrow\)}
\SetKwFor{function}{function}{:}{end} % Fuuuuuuj
\SetKwFunction{initialize}{initialize}
\SetKwFunction{predict}{predict}
\SetKwFunction{correct}{correct}
\SetKwFunction{resample}{resample}
\SetKwFunction{getAction}{getAction}
\SetKwFunction{getObservation}{getObservation}
\SetKwFunction{randomSample}{randomSample}
\SetKwFunction{sampleFromActionModel}{sampleFromActionModel}
\SetKwFunction{observationProbability}{observationProbability}
\SetKwFunction{outputPosition}{outputPosition}
\SetKwFunction{predictGPS}{predictGPS}
\SetKwFunction{observationProbabilityGPS}{observationProbabilityGPS}
\SetKwFunction{timeFunc}{time}
\SetKwFunction{delays}{delays}

\newcommand{\questiontodo}[1]{\todo[linecolor=green!50!white, backgroundcolor=green!50!white]{#1}}

\newcommand{\sirf}{SiRF~III }

\hyphenation{pseudo-range}
